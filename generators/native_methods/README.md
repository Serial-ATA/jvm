# JVM/generators/native_methods

This crate generates the native method lookup table in [runtime/native](../../runtime/src/native).

It is used in the [build script of the runtime crate](../../runtime/build.rs).