/// [**WINDOWS**] Get the size of a page in bytes
///
/// This shouldn't ever change during execution, so the result is cached after the first call.
pub fn get_page_size() -> usize {
	unimplemented!("Windows::mem::get_page_size")
}
